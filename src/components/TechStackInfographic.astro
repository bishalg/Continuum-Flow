---
---
<div class="tech-stack-container not-prose font-sans text-slate-200">
    <div class="max-w-5xl mx-auto space-y-12 py-12">

        <!-- Header -->
        <header class="text-center">
            <h1 class="text-4xl md:text-5xl font-black text-white mb-4 tracking-tight">
                THE SNOOPIEST TECH STACK
            </h1>
            <p class="text-slate-400 text-lg">
                Production-Grade Architecture for AI Video Generation
            </p>
        </header>

        <!-- LAYER 1: FRONTEND & ORCHESTRATION -->
        <section class="relative z-10">
            <div class="flex items-center gap-4 mb-4">
                <div class="h-px bg-slate-700 flex-1"></div>
                <h2 class="text-sky-400 font-bold uppercase tracking-widest text-sm">Layer 1: Interface & Control</h2>
                <div class="h-px bg-slate-700 flex-1"></div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <!-- Next.js -->
                <div class="tech-card border-t-4 border-white">
                    <div class="logo-box bg-white text-black">N</div>
                    <h3 class="font-bold text-lg">Next.js 14</h3>
                    <p class="text-xs text-slate-400 mt-1">App Router UI. Server Actions for direct backend calls.</p>
                </div>
                
                <!-- BullMQ -->
                <div class="tech-card border-t-4 border-red-500">
                    <div class="logo-box bg-red-500 text-white">Q</div>
                    <h3 class="font-bold text-lg">BullMQ / Redis</h3>
                    <p class="text-xs text-slate-400 mt-1">Async Job Queue. Handles long-running video generation tasks.</p>
                </div>

                <!-- Supabase -->
                <a href="/context-management" class="block">
                    <div class="tech-card border-t-4 border-green-500 hover:cursor-pointer">
                        <div class="logo-box bg-green-500 text-white">S</div>
                        <h3 class="font-bold text-lg">Supabase (PG)</h3>
                        <p class="text-xs text-slate-400 mt-1">Relational DB + pgvector. Stores World State & Character JSON.</p>
                        <div class="absolute top-2 right-2 text-green-400 text-xs">↗</div>
                    </div>
                </a>
            </div>
        </section>

        <!-- CONNECTOR -->
        <div class="h-12 relative">
            <div class="layer-connector h-full"></div>
        </div>

        <!-- LAYER 2: THE AI MODEL ZOO -->
        <section class="relative z-10">
            <div class="flex items-center gap-4 mb-4">
                <div class="h-px bg-slate-700 flex-1"></div>
                <h2 class="text-amber-400 font-bold uppercase tracking-widest text-sm">Layer 2: The Model Zoo (Cloud APIs)</h2>
                <div class="h-px bg-slate-700 flex-1"></div>
            </div>

            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                
                <!-- LLM -->
                <a href="/workflows" class="block">
                    <div class="tech-card bg-slate-800/50 hover:cursor-pointer h-full">
                        <div class="text-xs text-amber-500 font-bold mb-1">LOGIC / WRITING</div>
                        <h4 class="font-bold text-white">Claude 3.5 Sonnet</h4>
                        <div class="mt-2 text-[10px] bg-slate-900 p-1 rounded mono text-slate-400">Provider: Anthropic</div>
                        <div class="absolute top-2 right-2 text-amber-500 text-xs">↗</div>
                    </div>
                </a>

                <!-- IMAGE -->
                <a href="/character-consistency" class="block">
                    <div class="tech-card bg-slate-800/50 hover:cursor-pointer h-full">
                        <div class="text-xs text-green-400 font-bold mb-1">IMAGE GEN</div>
                        <h4 class="font-bold text-white">Flux.1 [Dev]</h4>
                        <div class="mt-2 text-[10px] bg-slate-900 p-1 rounded mono text-slate-400">Provider: Replicate</div>
                        <div class="absolute top-2 right-2 text-green-400 text-xs">↗</div>
                    </div>
                </a>

                <!-- VIDEO -->
                <a href="/character-consistency" class="block">
                    <div class="tech-card bg-slate-800/50 hover:cursor-pointer h-full">
                        <div class="text-xs text-purple-400 font-bold mb-1">VIDEO GEN</div>
                        <h4 class="font-bold text-white">Luma Dream Machine</h4>
                        <div class="mt-2 text-[10px] bg-slate-900 p-1 rounded mono text-slate-400">Provider: Luma Labs</div>
                        <div class="absolute top-2 right-2 text-purple-400 text-xs">↗</div>
                    </div>
                </a>

                <!-- AUDIO -->
                <div class="tech-card bg-slate-800/50 h-full">
                    <div class="text-xs text-blue-400 font-bold mb-1">VOICE / SYNC</div>
                    <h4 class="font-bold text-white">ElevenLabs + SyncLabs</h4>
                    <div class="mt-2 text-[10px] bg-slate-900 p-1 rounded mono text-slate-400">Provider: 11Labs/Sync</div>
                </div>

            </div>
        </section>

        <!-- CONNECTOR -->
        <div class="h-12 relative">
            <div class="layer-connector h-full"></div>
        </div>

        <!-- LAYER 3: ASSET & DOC MANAGEMENT -->
        <section class="relative z-10">
            <div class="flex items-center gap-4 mb-4">
                <div class="h-px bg-slate-700 flex-1"></div>
                <h2 class="text-teal-400 font-bold uppercase tracking-widest text-sm">Layer 3: Infrastructure & Docs</h2>
                <div class="h-px bg-slate-700 flex-1"></div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                
                <!-- The Quarto Pipeline -->
                <div class="tech-card quarto-section">
                    <div class="flex items-center gap-3 mb-3">
                        <div class="bg-teal-500 text-black font-bold px-2 py-1 rounded text-sm">QUARTO</div>
                        <h3 class="font-bold text-white">Executable Scripting</h3>
                    </div>
                    <p class="text-sm text-slate-400 mb-4">
                        Converts Markdown into Hybrid Documents.
                    </p>
                    <div class="bg-black/50 p-3 rounded font-mono text-xs text-teal-300 space-y-1 border border-teal-900/50">
                        <div>Input: story.md</div>
                        <div>Process: Agent injects {JSON} Metadata</div>
                        <div>Output 1: script.pdf (Human Review)</div>
                        <div>Output 2: script.json (Video Driver)</div>
                    </div>
                </div>

                <!-- The Asset Mirror -->
                <div class="tech-card border border-indigo-500/30">
                    <div class="flex items-center gap-3 mb-3">
                        <div class="bg-indigo-600 text-white font-bold px-2 py-1 rounded text-sm">ASSET SYNC</div>
                        <h3 class="font-bold text-white">Cloud-Local Mirror</h3>
                    </div>
                    
                    <div class="flex items-center justify-between gap-2 text-center text-xs">
                        <div class="bg-slate-900 p-2 rounded w-1/3 border border-slate-700">
                            <div class="font-bold text-white">GitHub</div>
                            <div class="text-slate-500">Code/Text</div>
                        </div>
                        <div class="text-indigo-400 font-bold text-lg">⇄</div>
                        <div class="bg-slate-900 p-2 rounded w-1/3 border border-slate-700">
                            <div class="font-bold text-white">Local CLI</div>
                            <div class="text-slate-500">Orchestrator</div>
                        </div>
                        <div class="text-indigo-400 font-bold text-lg">⇄</div>
                        <div class="bg-slate-900 p-2 rounded w-1/3 border border-slate-700">
                            <div class="font-bold text-white">AWS S3</div>
                            <div class="text-slate-500">Heavy Assets</div>
                        </div>
                    </div>
                    
                    <div class="mt-3 text-center">
                        <code class="bg-black text-indigo-300 px-2 py-1 rounded text-xs">npm run asset:sync</code>
                    </div>
                </div>

            </div>
        </section>

        <!-- FOOTER KEY -->
        <footer class="mt-16 text-center grid grid-cols-2 md:grid-cols-4 gap-4 text-xs text-slate-500">
            <div>🟦 Frontend (Next.js)</div>
            <div>🟪 Video (Luma)</div>
            <div>🟩 Database (Supabase)</div>
            <div>🟧 Writing (Claude)</div>
        </footer>

    </div>
</div>

<style>
    :global(.tech-stack-container) {
        --bg-color: #0B1120; /* Deep Navy Background */
        --card-bg: #1E293B;
        --primary: #38BDF8;   /* Sky 400 */
        --secondary: #2DD4BF; /* Teal 400 */
        --accent: #F472B6;    /* Pink 400 */
        --code: #F59E0B;      /* Amber 400 */
    }

    .mono { font-family: 'JetBrains Mono', monospace; }

    /* Tech Card Styling */
    .tech-card {
        background: var(--card-bg);
        border: 1px solid #334155;
        border-radius: 12px;
        padding: 1.25rem;
        position: relative;
        transition: all 0.2s ease;
    }
    .tech-card:hover {
        border-color: var(--primary);
        transform: translateY(-2px);
        box-shadow: 0 0 15px rgba(56, 189, 248, 0.15);
    }

    /* Logos (Simulated with Text/CSS) */
    .logo-box {
        width: 40px;
        height: 40px;
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 900;
        font-size: 1.2rem;
        margin-bottom: 0.75rem;
    }

    /* Connectors */
    .layer-connector {
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        width: 2px;
        background: #334155;
        z-index: 0;
    }

    /* Quarto Special Section */
    .quarto-section {
        background: linear-gradient(135deg, rgba(45, 212, 191, 0.1) 0%, rgba(15, 23, 42, 0) 100%);
        border: 1px dashed var(--secondary);
    }
</style>
